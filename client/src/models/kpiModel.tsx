export type KPI = {
    averageSalary: number,
    averageDuration: number,
    averageScore: number
}

export interface StateKpi {
    averageSalary: number,
    averageDuration: number,
    averageScore: number
}