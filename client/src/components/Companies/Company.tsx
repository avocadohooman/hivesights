import React from 'react';
import { Company } from '../../models/companyModel';

const Company = ({company}:{company: Company | undefined}):JSX.Element => {

    return (
        <div>Company</div>
    )
}