import pool from '../db';
import { CompanyTotalRating } from '../types/review';
import axios from 'axios';

const calculateTotalScore = async (companyId: string, companyRating: CompanyTotalRating) => {
    try {
        
    } catch (error) {
        
    }
}