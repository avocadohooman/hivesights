{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tsc": "tsc",
    "build:client": "rm -rf build && cd ../client && npm run build --prod && cp -r build ../server",
    "start": "ts-node index.ts",
    "start:server-dev": "NODE_ENV=server ts-node-dev index.ts",
    "start:dev": "NODE_ENV=development ts-node-dev index.ts",
    "start:prod": "NODE_ENV=production ts-node index.ts",
    "elint": "eslint --ext .ts .",
    "deploy": "git add . && git commit -m deploying && git push",
    "deploy:full": "npm run build-client && git add . && git commit -m uibuild_deploy && npm run deploy",
    "cypress:open": "npx cypress open",
    "test:e2e": "NODE_ENV=test cypress run",
    "test:int": "NODE_ENV=test jest --verbose --runInBand --detectOpenHandles"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.10",
    "@types/express": "^4.17.11",
    "@types/jest": "^26.0.23",
    "@typescript-eslint/eslint-plugin": "^4.24.0",
    "@typescript-eslint/parser": "^4.24.0",
    "cypress": "^7.3.0",
    "eslint": "^7.26.0",
    "jest": "^26.6.3",
    "supertest": "^6.1.3",
    "ts-jest": "^26.5.6",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "@types/pg": "^8.6.0",
    "@types/supertest": "^2.0.11",
    "cors": "^2.8.5",
    "dotenv": "^9.0.2",
    "express": "^4.17.1",
    "pg": "^8.6.0"
  }
}
